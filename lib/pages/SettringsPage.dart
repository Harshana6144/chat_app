import 'package:flutter/material.dart';

class Settringspage extends StatelessWidget {
  const Settringspage ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:Text("Settings")),
    );
  }
}